# Claude context

Follow `AGENTS.md`. The durable product is the semantic Agent Draw graph and capability model; tldraw is the first editor. Start in `README.md`, then `docs/FORMAT.md`, `src/model/schema.ts`, and `src/plugins/types.ts`.
